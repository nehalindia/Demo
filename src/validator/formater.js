const text = "     FunctionUp Technatium Backend Cohort   "

const upperCase = () => {console.log(text.toUpperCase())}

const lowerCase =() => {console.log(text.toLowerCase())}

const trim = () => {console.log(text.trim())}


module.exports = {upperCase, lowerCase,trim}